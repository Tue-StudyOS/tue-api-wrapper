name: TueAPI
options:
  bundleIdPrefix: dev.sebastianboehler
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    DEVELOPMENT_TEAM: 8P4NW2AQ75
    CODE_SIGN_STYLE: Automatic
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.9"
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
packages:
  mlx-swift-lm:
    url: https://github.com/ml-explore/mlx-swift-lm.git
    exactVersion: 3.31.3
  swift-huggingface:
    url: https://github.com/huggingface/swift-huggingface
    exactVersion: 0.9.0
  swift-transformers:
    url: https://github.com/huggingface/swift-transformers
    exactVersion: 1.3.0
targets:
  TueAPI:
    type: application
    platform: iOS
    sources:
      - path: TueAPI
      - path: Shared
    dependencies:
      - target: TueAPIWidgetExtension
        embed: true
      - package: mlx-swift-lm
        product: MLXLLM
      - package: mlx-swift-lm
        product: MLXLMCommon
      - package: swift-huggingface
        product: HuggingFace
      - package: swift-transformers
        product: Tokenizers
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.sebastianboehler.tueapi
        PRODUCT_NAME: TueAPI
        CODE_SIGN_ENTITLEMENTS: TueAPI/TueAPI.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
    info:
      path: TueAPI/Info.plist
      properties:
        CFBundleDisplayName: TUE API
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        GitHubFeedbackToken: "$(GITHUB_FEEDBACK_TOKEN)"
        ITSAppUsesNonExemptEncryption: false
        LSRequiresIPhoneOS: true
        NSCameraUsageDescription: "The camera is used to scan your Hochschulsport KuF and Deutschlandticket barcodes and save them locally on this device."
        NSLocationWhenInUseUsageDescription: "Your location is used to show your position on the campus map and help you navigate around Tübingen."
        NSSupportsLiveActivities: true
        UIApplicationSupportsIndirectInputEvents: true
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
  TueAPIWidgetExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: Widgets
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.sebastianboehler.tueapi.widget
        PRODUCT_NAME: TueAPIWidgetExtension
        CODE_SIGN_ENTITLEMENTS: Widgets/TueAPIWidgetExtension.entitlements
        APPLICATION_EXTENSION_API_ONLY: true
    info:
      path: Widgets/Info.plist
      properties:
        CFBundleDisplayName: TUE API Widgets
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        ITSAppUsesNonExemptEncryption: false
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
  TueAPITests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: TueAPITests
    dependencies:
      - target: TueAPI
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.sebastianboehler.tueapi.tests
        GENERATE_INFOPLIST_FILE: YES
schemes:
  TueAPI:
    build:
      targets:
        TueAPI: all
        TueAPIWidgetExtension: all
    test:
      targets:
        - TueAPITests

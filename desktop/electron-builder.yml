appId: de.sebastianboehler.tuestudyhub.desktop
productName: TUE Study Hub
directories:
  output: release
  buildResources: build
files:
  - dist-renderer/**/*
  - dist-electron/**/*
  - package.json
extraResources:
  - from: resources/backend
    to: backend
    filter:
      - '**/*'
asar: true
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
publish:
  provider: github
  owner: SebastianBoehler
  repo: tue-api-wrapper
mac:
  category: public.app-category.education
  icon: build/icon.icns
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  target:
    - dmg
win:
  icon: build/icon.ico
  target:
    - nsis
linux:
  icon: build/icon.png
  target:
    - AppImage
